name: Manual Build and Deploy

on:
  workflow_dispatch: {}

permissions:
  contents: write

env:
  PROJECT_NAME: the-forgotten-letters

jobs:
  buildForAndroid:
    name: Build for Android
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        targetPlatform:
          - Android # Build an Android .apk standalone app.
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - uses: jlumbroso/free-disk-space@v1.3.1

      - uses: actions/cache@v3
        with:
          path: Library
          key: Library-${{ matrix.targetPlatform }}
          restore-keys: Library-

      - name: Debug Itch.io Upload
        run: |
          echo "ITCH_USER: ${{ secrets.ITCH_USER }}"
          echo "ITCH_GAME: ${{ secrets.ITCH_GAME }}"

      - name: Install Butler
        run: |
          wget -q https://broth.itch.ovh/butler/linux-amd64/latest/butler -O butler
          chmod +x butler
          sudo mv butler /usr/local/bin/butler
          butler -V  # Verify installation

      - name: Verify Butler Installation
        run: |
          ls -lah /usr/local/bin/butler
          file /usr/local/bin/butler
          /usr/local/bin/butler -V


      - uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: ${{ matrix.targetPlatform }}
          buildName: ${{ env.PROJECT_NAME }}
          buildsPath: build/

      - name: Check if APK exists
        run: |
          if [ ! -f "build/${{ matrix.targetPlatform }}/${{ env.PROJECT_NAME }}.apk" ]; then
            echo "❌ Error: APK file not found!"
            exit 1
          fi

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v1.0.${{ github.run_number }}
          release_name: "Release v1.0.${{ github.run_number }}"
          body: "Automated release for the latest Android build."
          draft: false
          prerelease: false

      - name: Upload APK to GitHub Release
        if: steps.create_release.outputs.upload_url != ''
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: build/Android/${{ env.PROJECT_NAME }}.apk
          asset_name: ${{ env.PROJECT_NAME }}.apk
          asset_content_type: application/vnd.android.package-archive

      - name: Upload to Itch.io
        run: |
          butler push build/Android/${{ env.PROJECT_NAME }}.apk ${{ secrets.ITCH_USER }}/${{ secrets.ITCH_GAME }}:android
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_CREDENTIALS }}
